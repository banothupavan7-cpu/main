//Access git commands

$ git init
$ git add <filename>
$ git status
$ git commit -m "your message"
$ git push


$ git clone https://git@github <username>/<repo>.git
$ git

$ git remote set-url origin https://github.com/username/repo.git

$ git branch --show-current
$ git checkout -b main


$ git status

$ git login

$ git remote -v
$ git pull --rebase origin <branch name>
$ git add <resolved-file>
$ git rebase --continue
$ git commit -m "your message"
$ git push origin main

//basic commands

$ git mv oldfilename newfilename //rename file in git


$ git rm filename // delete the file in Git
$ git rm path/to/your file // remove the file
$ git 
